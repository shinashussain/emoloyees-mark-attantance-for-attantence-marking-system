import 'package:flutter/material.dart';

class MonthlyRecord extends StatelessWidget {
  const MonthlyRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Monthly Record'),
    );
  }
}
